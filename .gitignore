# Data files (Exclude large CSVs and processed parquets)
data/*.csv
data/processed/*.parquet

# Model artifacts
models/*.json
models/*.pkl

# Outputs (Logs, generated parquet files for dashboard)
# We keep output parquets because they are tiny and required for the dashboard.
# We can keep reports/ JSONs since they are small metrics

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Virtual environments
venv/
env/
.env

# IDEs
.vscode/
.idea/
